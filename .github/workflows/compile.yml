name: Compile Java
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Compile
        run: |
          mkdir -p lib
          wget https://repo1.maven.org/maven2/org/java-websocket/Java-WebSocket/1.5.3/Java-WebSocket-1.5.3.jar -O lib/Java-WebSocket.jar
          javac -cp "lib/Java-WebSocket.jar" OldMaidApp.java
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: class-files
          path: "*.class"
